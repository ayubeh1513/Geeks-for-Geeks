class Solution {
    static boolean isPrime(int n) {
        // code here
        int d=0;
        if(n==1) return false;
        for(int i=1; i*i<=n; i++){
            if(n%i==0) d++;
            if(d>1) return false;
        }
        
        return true;
    }
}
